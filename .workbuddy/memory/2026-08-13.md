# 2026-08-13

## 博客部署到 GitHub Pages(完成)

- 仓库: https://github.com/cjw061425/stepchen.git
  - `main` 分支存 Hexo 源码(备份),`gh-pages` 分支存编译产物(Pages 发布源)
  - 发布 URL: https://cjw061425.github.io/stepchen/ (自定义域名 stepchen.com 待 DNS 生效)
- 关键修复:
  - `themes/prince` 原是孤儿 gitlink(无 .gitmodules),已并入主仓库为普通目录;原 .git 备份在 /tmp/themes-prince-git-backup
  - `_config.yml` deploy 段修复:YAML 语法错误(type:git)、拼写错误(gighub.com → github.com)、branch 改为 gh-pages
  - `source/CNAME` = stepchen.com,hexo generate 会复制到 public/ 根目录,GitHub Pages 自动识别
- 操作经验:
  - 沙箱网络无法访问 GitHub(代理隧道 502、直连超时),git push / hexo deploy 必须用非沙箱模式跑
  - 本机无 git user.name/email 配置,hexo-deployer-git 在 .deploy_git 提交会报 "unable to auto-detect email";用环境变量注入:
    `GIT_AUTHOR_NAME/EMAIL + GIT_COMMITTER_NAME/EMAIL`,名字 Stephen Chen,邮箱 cjw061425@users.noreply.github.com
  - 构建命令: `./node_modules/.bin/hexo clean && ./node_modules/.bin/hexo generate`(用项目本地 hexo)
