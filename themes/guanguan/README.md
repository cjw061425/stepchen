# Guanguan

A SIMPLE and CLEAN theme for [Hexo].

*No categories. No share. No comment.*

## Installation

### Install

``` bash
$ npm install hexo-renderer-jade --save
$ git clone <your-repo-url> themes/guanguan
```

### Enable

Modify `theme` setting in `_config.yml` to `guanguan`.
